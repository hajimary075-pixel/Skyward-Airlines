{
  "name": "skyward-airlines-site",
  "version": "1.0.0",
  "description": "Original airline/travel booking website (config-driven branding, zero external dependencies)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" }
}
